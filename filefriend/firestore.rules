rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /shares/{code} {
      allow read, write: if request.auth != null && code.matches('^[0-9]{3}$');
    }
  }
}
